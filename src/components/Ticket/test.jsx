import React from 'react';
import PropTypes from 'prop-types';

const Test = props => {
    return (
        <div>

        </div>
    );
};

Test.propTypes = {
    task: PropTypes.object
};

export default Test;